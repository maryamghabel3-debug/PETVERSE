name: deploy-preview
on:
  push:
    branches: [main]
jobs:
  deploy-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v3
        with: {version: 9}
      - uses: actions/setup-node@v4
        with: {node-version: 20, cache: pnpm}
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter=web build
      # Vercel deploy – needs VERCEL_TOKEN secret
      # - run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }} --cwd apps/web --yes
  deploy-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Railway deploy – needs RAILWAY_TOKEN
      # - uses: railwayapp/action@v1
