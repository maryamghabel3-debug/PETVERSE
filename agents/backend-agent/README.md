# Backend Agent
NestJS + Prisma + Postgres + Redis + Socket.IO
ماژول‌ها:
- auth (OAuth2.1/OIDC, WebAuthn)
- users / pets
- petconnect (feed, chat E2E, clubs)
- vetcare (booking, LiveKit)
- commerce (visual search, price compare)
- aipal (proxy)
امنیت: GDPR/HIPAA-aligned, E2E chat, rate-limit
